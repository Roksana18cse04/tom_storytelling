version: '3.8'

services:
  app:
    build: .
    container_name: tom_storytelling
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./user_images:/app/user_images
    restart: unless-stopped
